#!/bin/bash
set -e

sudo apt update
sudo apt install -y maven

sudo mysql <<EOF
CREATE DATABASE IF NOT EXISTS student_db;
CREATE USER IF NOT EXISTS 'aashiq'@'localhost' IDENTIFIED BY 'aashiq123';
GRANT ALL PRIVILEGES ON student_db.* TO 'aashiq'@'localhost';
FLUSH PRIVILEGES;
EOF

cd /home/aashiq/SpringBoot-Application/Parag_StudentMgmtSystem || exit 1

mvn clean install -DskipTests

